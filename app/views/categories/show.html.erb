<div class="navSpacer"></div>

<div class="container">

  <h2><%= @category.name %></h2>

  <% if user_signed_in? && current_user.admin? %>
    <%= button_to 'delete', @category, :method => 'delete'%>
  <% end %>

</div>
