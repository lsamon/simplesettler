<% articles.each do | article | %>
  <div class='articleCont'>
    <%= link_to article.title, city_article_path(current_city, article), class: 'articleSelect' %>
    <%= image_tag article.image.detail.url, class: "articleImg" %>
  </div>
<% end %>
