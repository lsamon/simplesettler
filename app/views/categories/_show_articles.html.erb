<% articles.each do | article | %>
  <div class='articleCont'>
    <%= link_to article.title, article_path(article), class: 'articleSelect' %>
    <%= image_tag article.image, class: "articleImg" %>
  </div>
<% end %>
