<div class="navSpacer"></div>

<div class="container">

  <h2><%= @city.name %></h2>
  <p><%= @city.description %></p>

  <% if @current_user.present? && @current_user.admin? %>
    <%= button_to 'delete', @city, :method => 'delete'%>
  <% end %>

  <div>
    <div class="categorySelect">
      <%= link_to 'All', city_articles_category_path(@city, 'All'), :remote => true  %>
    </div>
    <% @categories.each do |c| %>
      <div class="categorySelect">
        <%= link_to c.name, city_articles_category_path(@city, c.name), :remote => true %>
      </div>
    <% end %>
  </div>

  <hr>
  <div class="articleSelectContainer">
    <% @articles.each do |a| %>
    <div class='articleSelect'>
      <%= a.title %>
    </div>
    <% end %>
  </div>

</div>
