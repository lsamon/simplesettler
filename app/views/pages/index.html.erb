<div id="countryModal" class="modal fade" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Sign up for future updates</h4>
      </div>
      <div class="modal-body">
        <p>Unfortunately your country is not available yet, register to be notified of any future changes.</p>
        <form>
          <div class="form-group">
           <label for="full_name">Full name:</label>
            <input type="text" id="full_name" class="form-control">
            </div>
            <div class="form-group">
            <label for="email">Email address:</label>
            <input type="email" id="email" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- end modal code -->
<!-- NOTE:  $('#myModal').modal('toggle') can be used to trigger modal-->


<!-- Modal button for testing purposes
<button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#countryModal">Open Modal</button>
 -->


 <div class="banner">
  <div>
    <h1>SETTLE ANYWHERE.</h1>
    <h2>Discover and connect with services in your city.</h2>
    <div>
    <%= form_tag(cities_path, :method => "get", id: "search-form") do %>
      <%= text_field_tag :search, params[:search], placeholder: "Search Cities", :class => 'searchCity', :pattern => ".{2,20}", :title => 'Enter more than 1 character', data: { autocomplete: autocomplete_city_name_cities_path } %>
      <%= button_to nil, :class => 'searchCitySubmit' do %><i class="fa fa-search" aria-hidden="true"></i><% end %><br />
    <% end %>
  </div>

    <div class="ausFlag"></div>
    <div class="flagAvail"><em><p>Currently available in Australia</p></em></div>
  </div>
</div>


<div class="container">

  <div class="explore">

    <div class="header">
      <h2>Explore your community</h2>
      <p>See whats happening in your community, or find new citys to explore.</p>
    </div>


  <div class="cities">
     <% @cities.in_groups_of(3)  do |c1, c2, c3| %>
      <div class="row">
          <div class="col-sm-4 <%= c1.name %>">
              <%= link_to city_path(c1), :class => 'labelShort' do %>
              <span><%= "#{(c1.name).upcase}" %></span>
              <%= image_tag "#{c1.name.downcase}.jpg", :class => "" %>
              <% end %>
          </div>
          <% if c2 %>
          <div class="col-sm-4 <%= c2.name %>">
              <%= link_to city_path(c2), :class => 'labelShort' do %>
              <span><%= "#{(c2.name).upcase}" %></span>
              <%= image_tag "#{c2.name.downcase}.jpg", :class => "" %>
              <% end %>
          </div>
          <% end %>
          <% if c3 %>
          <div class="col-sm-4 <%= c3.name %>">
              <%= link_to city_path(c3), :class => 'labelShort' do %>
              <span><%= "#{(c3.name).upcase}" %></span>
              <%= image_tag "#{c3.name.downcase}.jpg", :class => "" %>
              <% end %>
          </div>
          <% end %>
      </div>
      <% end %>
  </div>

  </div>
</div>
<hr>
<div class="container">
  <!-- show categories as a carousel -->
  <div class="categories">
    <%#= render categories %>
    <h1>Add Categories Carousel</h1>
  </div>
</div>

<div class="getStarted">
  <%= render 'getstarted' %>
</div>

<div class="experts">
  <h2>Speak to our awesome experts</h2>
  <%= render 'speak_to_experts' %>
</div>

<div class="container">
  <div id="howItWorks" class="howItWorks">
    <%= render 'how_it_works' %>
  </div>

  <div class="settlerLove">
    <%= render 'love_simplesettler' %>
  </div>

</div>



<div class="recommendedBy">
  <%= render 'media' %>
</div>


<div class="pageEnd">
  <div class="container">
    <div class="content">
      <h1>YOUR STRESS ENDS HERE. LET US HELP YOU.</h1>
      <%=  link_to "Search Your City", nil, :class => "searchCity button"%>
      <%=  link_to "Visa Help", nil, :class => "VisaHelp button"%>
    </div>
  </div>
</div>
