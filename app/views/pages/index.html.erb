<div id="countryModal" class="modal fade" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">Sign up for future updates</h4>
      </div>
      <div class="modal-body">
        <p>Unfortunately your country is not available yet, register to be notified of any future changes.</p>
        <form>
          <div class="form-group">
           <label for="full_name">Full name:</label>
            <input type="text" id="full_name" class="form-control">
            </div>
            <div class="form-group">
            <label for="email">Email address:</label>
            <input type="email" id="email" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
      </div>
    </div>
  </div>
</div>

<!-- end modal code -->
<!-- NOTE:  $('#myModal').modal('toggle') can be used to trigger modal-->


<!-- Modal button for testing purposes
<button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#countryModal">Open Modal</button>
 -->


 <div class="row">
  <div class="col-sm-12 banner">
    <%= image_tag "slider2.jpg", title: 'homepage banner', alt: 'banner' %>
    <div row>
      <div class="col-sm-12 content">
        <h1><%= t('site.header.title') %></h1>
        <h2><%= t('site.header.subtitle') %></h2>
        <%= form_tag(cities_path, method: "get", id: "search-form") do %>
          <%= text_field_tag :search, params[:search], placeholder: t('site.header.search.placeholder'), class: 'searchCity', pattern: ".{2,20}", title: 'Enter more than 1 character', data: { autocomplete: autocomplete_city_name_cities_path } %>
          <%= button_to nil, class: 'searchCitySubmit' do %><i class="fa fa-search" aria-hidden="true"></i><% end %><br />
        <% end %>
        <div class="ausFlag"></div>
        <div class="flagAvail">
          <em><p><%= t('site.header.currently_available') %></p></em>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="row explore">
  <div class="col-sm-12 header">
    <h2><%= t('site.cities.title') %></h2>
    <p><%= t('site.cities.subtitle') %></p>
  </div>
</div>

<div class="row cities bottom-buffer">
  <div class="col-sm-12">
    <div class="row">
     <% @cities.each  do |city| %>
        <div class="col-xs-12 col-lg-4 bottom-buffer">
            <%= link_to city_path(city) do %>
              <h2><%= city.name.upcase %></h2>
              <%= image_tag "#{city.name.downcase}.jpg" %>
            <% end %>
        </div>
    <% end %>
    </div>
  </div>
</div>

<%= render 'speak_to_experts' %>
<%= render 'how_it_works' %>
<%= render 'page_end' %>
<%= render 'media' %>
